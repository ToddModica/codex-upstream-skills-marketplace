# Assets

`itasca-mcp-addon.py` is the upstream MIT-licensed bridge addon needed inside
an ITASCA engine session. The plugin MCP configuration itself starts the public
`itasca-mcp` package using `uvx`.
